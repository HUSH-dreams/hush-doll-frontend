// MapIconsOverlay.js
import React from 'react';

const MapIconsOverlay = ({ mapRect, mapElements, chosen }) => {
    if (!mapRect) {
        return null;
    }

    return null;
};

export default MapIconsOverlay;